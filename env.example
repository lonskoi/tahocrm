## tahoCRM example environment variables
## Copy to .env and adjust values for your machine

# Postgres (docker-compose.yml default)
# Multi-DB tenancy model:
# - master DB (platform / mega-admin): tahocrm_master
# - tenant DB per workshop: tahocrm_tenant_<tenantId>
DATABASE_URL_ADMIN="postgresql://user:password@localhost:5432/postgres"
DATABASE_URL_MASTER="postgresql://user:password@localhost:5432/tahocrm_master"

# Used by Prisma CLI (we switch it in scripts when pushing schema to specific DB)
DATABASE_URL="postgresql://user:password@localhost:5432/tahocrm_master"

# Dev convenience: default tenant to use for demo users / local setup
# NOTE: matches the hardcoded demo manager user in lib/auth.ts and existing tests.
DEV_DEFAULT_TENANT_ID="tenant-1"

# Optional (dev only): allow creating a new tenant DB from mega-admin API (spawns Prisma CLI)
ALLOW_DB_PROVISIONING="1"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
# Generate a strong secret (example): node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="change-me-in-local-env"

# Logging (optional)
LOG_LEVEL="debug"

# Optional: bootstrap admin creation script
ADMIN_EMAIL="admin@tahocrm.ru"
ADMIN_PASSWORD="admin123"
ADMIN_NAME="Администратор"


