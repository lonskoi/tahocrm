# Copy to .env on the VPS (same folder as docker-compose.prod.yml) and replace values.

# Docker image to run (from GHCR)
APP_IMAGE="ghcr.io/OWNER/REPO:latest"

# Postgres credentials (used by the db container)
POSTGRES_USER="tahocrm"
POSTGRES_PASSWORD="change-me-strong"

# App / NextAuth
NEXTAUTH_URL="http://46.173.19.235"
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="change-me-strong"

# Multi-DB tenancy model
# - master DB: tahocrm_master
# - tenant DB per workshop: tahocrm_tenant_<tenantId>
DATABASE_URL_ADMIN="postgresql://tahocrm:change-me-strong@db:5432/postgres"
DATABASE_URL_MASTER="postgresql://tahocrm:change-me-strong@db:5432/tahocrm_master"
# Prisma CLI uses DATABASE_URL by default
DATABASE_URL="postgresql://tahocrm:change-me-strong@db:5432/tahocrm_master"

# Default tenant for initial bootstrap
DEV_DEFAULT_TENANT_ID="tenant-1"

# Optional: bootstrap tenant admin (used by scripts/create-tenant-admin.ts)
TENANT_ID="tenant-1"
ADMIN_EMAIL="admin@tahoerp.ru"
ADMIN_PASSWORD="change-me-strong"
ADMIN_NAME="Администратор"
TENANT_NAME="Мастерская tenant-1"

