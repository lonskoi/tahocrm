# Быстрый старт: Логирование и проверка

> Актуально: текущий `lib/logger.ts` выводит логи в stdout/stderr (консоль/логи контейнера).
> Если файловый транспорт не подключен отдельно, `logs/application-*.log` и `logs/error-*.log` могут не создаваться.

## 🚀 Запуск проверки

```bash
# Полная проверка всего приложения
npm run check-all
```

## 📝 Логирование

### Где смотреть логи

Логи сохраняются в папке `logs/`:

- `application-YYYY-MM-DD.log` - все логи
- `error-YYYY-MM-DD.log` - только ошибки

### Просмотр в реальном времени

```bash
# Windows PowerShell
Get-Content logs\application-*.log -Wait -Tail 50

# Linux/Mac
tail -f logs/application-*.log
```

## 🔍 Поиск проблем

### Найти все ошибки за сегодня

```bash
grep "$(date +%Y-%m-%d)" logs/error-*.log
```

### Найти ошибки конкретного пользователя

```bash
grep "userId.*user-123" logs/application-*.log
```

### Найти медленные запросы (>1 секунды)

```bash
grep "duration.*[0-9][0-9][0-9][0-9]ms" logs/application-*.log
```

## ✅ Проверка перед коммитом

При каждом коммите автоматически:

1. Проверяется код линтером
2. Форматируется код
3. Проверяются типы

Если что-то не так - коммит не пройдет.

## 🧪 Запуск тестов

```bash
# Все тесты
npm run test

# С покрытием
npm run test:coverage

# В режиме наблюдения
npm run test:watch
```

## 📊 Что проверяется

### Автоматически

- ✅ Типы TypeScript
- ✅ Стиль кода (ESLint)
- ✅ Форматирование (Prettier)
- ✅ Тесты (Jest)
- ✅ Сборка (Next.js)

### В логах

- ✅ Все API запросы
- ✅ Все ошибки
- ✅ Длительность операций
- ✅ Действия пользователей
- ✅ Изменения данных

## 🎯 Примеры использования

### Добавить логирование в новый API route

```typescript
import { logger, logApiRequest } from '@/lib/logger'
import { handleApiError, ApiError } from '@/lib/api-error-handler'

export async function GET(request: NextRequest) {
  const startTime = Date.now()
  const path = request.nextUrl.pathname

  try {
    logger.info('GET /api/example - Starting request', { path })

    // Ваш код...

    logger.info('GET /api/example - Success', {
      duration: `${Date.now() - startTime}ms`,
    })

    return NextResponse.json(data)
  } catch (error) {
    return handleApiError(error, {
      method: 'GET',
      path,
    })
  }
}
```

### Добавить тест

```typescript
// __tests__/api/example.test.ts
describe('API Example', () => {
  it('should return 200', async () => {
    const response = await GET(request)
    expect(response.status).toBe(200)
  })
})
```

## ⚠️ Важно

1. **Всегда логируйте начало и конец операций**
2. **Включайте контекст (userId, tenantId)**
3. **Логируйте длительность**
4. **Не логируйте пароли и токены**
5. **Используйте правильные уровни (info, warn, error)**

## 📚 Документация

- `README_LOGGING.md` - полная документация по логированию
- `CHECKLIST.md` - чеклист проверок
- `jest.config.js` - конфигурация тестов
- `.eslintrc.json` - правила линтера
